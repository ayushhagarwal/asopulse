{
  "name": "@asopulse/providers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check src",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "@asopulse/domain": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
