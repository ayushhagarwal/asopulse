# Contributing

Use Node 20 or newer and pnpm 11. Create focused commits, add tests for behavioral changes,
and run `pnpm check && pnpm typecheck && pnpm test && pnpm build` before opening a pull request.

ASOpulse does not accept proprietary data dumps, copied competitor assets, undisclosed scraping,
or metrics that cannot explain their source and method.
