name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: SemVer tag, for example v0.1.0 or v0.1.0-rc.1
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  validate:
    name: validate-release
    runs-on: ubuntu-latest
    timeout-minutes: 20
    outputs:
      version: ${{ steps.version.outputs.version }}
      stable: ${{ steps.version.outputs.stable }}
      major: ${{ steps.version.outputs.major }}
      minor: ${{ steps.version.outputs.minor }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          ref: main
          fetch-depth: 0
      - name: Validate version and main
        id: version
        env:
          VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          [[ "$VERSION" =~ ^v(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(-[0-9A-Za-z.-]+)?$ ]] || { echo "Invalid SemVer tag" >&2; exit 1; }
          if git ls-remote --exit-code --tags origin "refs/tags/$VERSION" >/dev/null 2>&1; then
            echo "Tag already exists" >&2
            exit 1
          fi
          test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)"
          core="${VERSION#v}"
          core="${core%%-*}"
          IFS=. read -r major minor _ <<< "$core"
          {
            echo "version=$VERSION"
            echo "major=v$major"
            echo "minor=v$major.$minor"
            [[ "$VERSION" == *-* ]] && echo "stable=false" || echo "stable=true"
          } >> "$GITHUB_OUTPUT"
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
        with:
          version: 11.8.0
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm check
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm build
      - run: pnpm audit --audit-level=high
      - run: pnpm licensecheck

  publish-images:
    name: publish-${{ matrix.image }}
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: api
            file: Dockerfile
            target: api
          - image: worker
            file: Dockerfile
            target: worker
          - image: web
            file: apps/web/Dockerfile
            target: ""
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          ref: main
      - uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8 # v4.2.0
      - uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0
      - uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0 # v4.4.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Prepare image tags
        id: tags
        env:
          IMAGE: ghcr.io/${{ github.repository_owner }}/asopulse-${{ matrix.image }}
          VERSION: ${{ needs.validate.outputs.version }}
          STABLE: ${{ needs.validate.outputs.stable }}
          MAJOR: ${{ needs.validate.outputs.major }}
          MINOR: ${{ needs.validate.outputs.minor }}
        run: |
          {
            echo 'tags<<EOF'
            echo "$IMAGE:$VERSION"
            echo "$IMAGE:sha-${GITHUB_SHA::12}"
            if [[ "$STABLE" == "true" ]]; then
              echo "$IMAGE:$MINOR"
              echo "$IMAGE:$MAJOR"
              echo "$IMAGE:latest"
            fi
            echo EOF
          } >> "$GITHUB_OUTPUT"
      - name: Build and publish
        id: build
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          file: ${{ matrix.file }}
          target: ${{ matrix.target }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          sbom: true
          provenance: mode=max
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - uses: actions/attest-build-provenance@96b4a1ef7235a096b17240c259729fdd70c83d45 # v2
        with:
          subject-name: ghcr.io/${{ github.repository_owner }}/asopulse-${{ matrix.image }}
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true

  publish-release:
    name: publish-github-release
    needs: [validate, publish-images, scan-published-images]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          ref: main
          fetch-depth: 0
      - name: Create annotated tag
        env:
          VERSION: ${{ needs.validate.outputs.version }}
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git tag -a "$VERSION" -m "ASOpulse $VERSION"
          git push origin "$VERSION"
      - name: Create GitHub release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          tag_name: ${{ needs.validate.outputs.version }}
          name: ASOpulse ${{ needs.validate.outputs.version }}
          generate_release_notes: true
          prerelease: ${{ needs.validate.outputs.stable != 'true' }}
          body: |
            See [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/${{ needs.validate.outputs.version }}/CHANGELOG.md) and the [self-hosting upgrade guide](https://github.com/${{ github.repository }}/blob/${{ needs.validate.outputs.version }}/docs/self-hosting.md).

            Published images: `asopulse-api`, `asopulse-worker`, and `asopulse-web` on `ghcr.io/${{ github.repository_owner }}`.

  scan-published-images:
    name: scan-published-${{ matrix.image }}
    needs: [validate, publish-images]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      packages: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        image: [api, worker, web]
    steps:
      - uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0 # v4.4.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Scan published image
        uses: anchore/scan-action@e1165082ffb1fe366ebaf02d8526e7c4989ea9d2 # v7.4.0
        with:
          image: ghcr.io/${{ github.repository_owner }}/asopulse-${{ matrix.image }}:${{ needs.validate.outputs.version }}
          fail-build: true
          severity-cutoff: high
          only-fixed: true
          output-format: sarif
          output-file: results.sarif
      - name: Upload scan result
        uses: github/codeql-action/upload-sarif@02c5e83432fe5497fd85b873b6c9f16a8578e1d9 # v3
        with:
          sarif_file: results.sarif
