{
  "name": "asopulse",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "format": "biome format --write .",
    "check": "biome check .",
    "licensecheck": "pnpm licenses list --prod --json | node scripts/check-licenses.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@types/node": "^24.10.1",
    "turbo": "^2.10.4",
    "typescript": "^7.0.2"
  }
}
