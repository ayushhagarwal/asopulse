# ADR 0001: Public data only in V1

V1 uses Apple's public Search API and requires no Apple credentials. App Store Connect, Apple Ads,
reviews, competitors, and private data providers are deferred. Unsupported metrics are shown as
unavailable rather than estimated from unrelated inputs.
